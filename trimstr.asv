function out_char = trimstr(in_char)

out_char = in_char;
last_char = in_char(length(in_char));


while (last_char == ' ') 
    out_char = in_char(1:(length(in_char)-1));
    last_char = in_char(length(in_char));
end